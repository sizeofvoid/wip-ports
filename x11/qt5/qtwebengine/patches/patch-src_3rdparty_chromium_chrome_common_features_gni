Index: src/3rdparty/chromium/chrome/common/features.gni
--- src/3rdparty/chromium/chrome/common/features.gni.orig
+++ src/3rdparty/chromium/chrome/common/features.gni
@@ -63,7 +63,7 @@ declare_args() {
   # It is enableable separately to facilitate testing.
   enable_hangout_services_extension = is_chrome_branded
 
-  enable_service_discovery = (enable_mdns && !is_android) || is_mac
+  enable_service_discovery = (enable_mdns && !is_android) || is_mac || is_bsd
 
   # Enables use of the session service, which is enabled by default.
   # Android stores them separately on the Java side.
